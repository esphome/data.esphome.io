{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://data.esphome.io/components.schema.json",
  "title": "Components",
  "description": "List of all components in ESPHome",
  "type": "object",
  "properties": {
    "components": {
      "description": "The components",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "target_platforms": {
      "description": "Components that are target platforms (have IS_TARGET_PLATFORM = True)",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "platform_components": {
      "description": "Components that are platform components (have IS_PLATFORM_COMPONENT = True)",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "last_updated": {
      "description": "The date when this file was last updated",
      "type": "string",
      "format": "date-time"
    },
    "version": {
      "description": "The ESPHome version this file is for",
      "type": "string"
    }
  },
  "required": [
    "components",
    "target_platforms",
    "platform_components",
    "last_updated",
    "version"
  ],
  "additionalProperties": false
}
